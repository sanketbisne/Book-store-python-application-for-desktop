import tkinter
m=tkinter.Tk() where m is the name of the main window object
m.mainloop()

// THE APPLICATION USES CRUD OPERATION , AS A WHOLE..
//USER CAN ADD BOOK ENTRIES, UPDATE THEM, DELETE THEM , ADD THEM INTO DATABASE..AS PER THIER NEEDS
//using the tkinter we can create web desktop application graphical user interface . 
